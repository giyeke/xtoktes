apiVersion: v1
kind: Service
metadata:
  name: arouck
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "arouck"
    port: 8080
  selector:
    app: arouck
