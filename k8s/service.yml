apiVersion: v1
kind: Service
metadata:
  name: seempo
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "seempo"
    port: 8080
  selector:
    app: seempo
