apiVersion: v1
kind: Service
metadata:
  name: devcke
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "devcke"
    port: 8080
  selector:
    app: devcke
