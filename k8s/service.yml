apiVersion: v1
kind: Service
metadata:
  name: berek
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "berek"
    port: 8080
  selector:
    app: berek
