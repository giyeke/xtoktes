apiVersion: v1
kind: Service
metadata:
  name: catio
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "catio"
    port: 8080
  selector:
    app: catio
