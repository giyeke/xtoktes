apiVersion: v1
kind: Service
metadata:
  name: secatio
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "secatio"
    port: 8080
  selector:
    app: secatio
